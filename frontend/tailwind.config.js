/** @type {import('tailwindcss').Config} */
const tailwindConfig = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Ensure correct paths to your project files
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default tailwindConfig; // Export with a named variable
